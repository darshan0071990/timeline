extends layout

block content
  .row
    .col-xs-12
      br
      table.table
        thead
          tr: th Project ID
            th Name
            th Users
            th Color
        for projects in data
          tr
            td= projects.id
            td= projects.name
            td= projects.color
